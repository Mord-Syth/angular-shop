import { ColorClickDirective } from './color-click.directive';

describe('ColorClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorClickDirective();
    expect(directive).toBeTruthy();
  });
});
